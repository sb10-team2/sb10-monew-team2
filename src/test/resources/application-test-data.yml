spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/monew?reWriteBatchedInserts=true
    username: monew
    password: monew123
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: none
    show-sql: false
